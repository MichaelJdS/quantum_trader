global:
  scrape_interval: 1s # HFT exige coleta a cada 1 segundo
  evaluation_interval: 1s

scrape_configs:
  - job_name: 'quantum_bot'
    static_configs:
      # O host.docker.internal é a ponte mágica que faz o Docker enxergar o seu Windows
      - targets: ['host.docker.internal:8000']